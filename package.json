{
  "name": "musicmap-root",
  "version": "1.0.0",
  "description": "MusicMap root runner",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix frontend",
    "dev": "concurrently -n \"server,frontend\" -c \"magenta,cyan\" \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix server && npm run build --prefix frontend",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
